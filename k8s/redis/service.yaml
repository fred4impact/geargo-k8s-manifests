apiVersion: v1
kind: Service
metadata:
  name: redis
  labels:
    app: geargo
    component: redis
spec:
  selector:
    app: geargo
    component: redis
  ports:
  - name: redis
    port: 6379
    targetPort: 6379
  type: ClusterIP
